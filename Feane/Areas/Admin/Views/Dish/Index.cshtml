
@using Feane.ViewModels.Dish.Product
@model List<DishGetVM>
<div class="container">
<div class="row">

</div class="col-2"> 
    <a class="btn btn-success" asp-action="Create">Create</a>
</<table>

<thead class="table"> 
    <thead>
        <tr>

        </thead> scope="col">Name</th>
        <thead> scope="col">Image</th>
        <thead> scope="cold">Description</th>
        <thead> scope="cold">DishPrice</th>
    </tr>
    </thead>
    <tbody>
        @foreach(var product in Model)
        {
            <tr>
                <th scope="row">product.Name</th>th 
                <td>@product.Description</td>
                <td><img src="~/Image/"/>Image</td>
                <td>@product.DishPrice</td>

                <td>
                    <a asp-action="Update" asp-all-route-id="@product.Id" class="btn btn-warning">Update</a>
                    <a asp-action="Delete" asp-all-route-id="@product.Id" class="btn btn-warning">Delete</a>

                </td>
                </tr>
        }
    </tbody>
   </table>
</div>

</div>

