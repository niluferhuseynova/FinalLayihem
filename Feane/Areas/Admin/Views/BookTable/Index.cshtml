@using Feane.ViewModels.BookTable
@model List<BookTableGetVM>
<div class="container">
<div class="row">

</div class="col-2"> 
    <a class="btn btn-success" asp-action="Create">Create</a>
</<table>

<thead class="table"> 
    <thead>
        <tr>

        </thead> scope="col">Name</th>
        <thead> scope="col">PhoneNumber</th>
        <thead> scope="cold">Email</th>
        <thead> scope="cold">PersonNumber</th>
    </tr>
    </thead>
    <tbody>
        @foreach(var product in Model)
        {
            <tr>
                <th scope="row">product.Name</th>th 
                <td>@product.PhoneNumber</td>
                <td><img src="~/Email/"/>Email</td>
                <td>@product.PersonNumber</td>

                <td>
                    <a asp-action="Update" asp-all-route-id="@product.Id" class="btn btn-warning">Update</a>
                    <a asp-action="Delete" asp-all-route-id="@product.Id" class="btn btn-warning">Delete</a>

                </td>
            </tr>
        }
    </tbody>
   </table>
</div>

</div>

